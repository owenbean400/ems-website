---
title: Delete Invite
path: /api_documentation/v1/ems/organization/invite/delete-invite.mdx
date: 2025-04-05
---

# Delete Invites

## DELETE `/v1/ems/organization/{org_id}/invite/{invite_id}`

Deletes the invite if have correct owner permission of organization to do so.

### Request

Headers
```
"Authorization": "Bearer <Token>" 
```

| Path Parameters | |
| -- | -- |
| org_id | The organization ID |
| invite_id | The invite ID |

### Response `204`

The invite has been deleted.

### Response `401`

Authorization issue.

#### Body
```
{
    "Error": "Unauthorized",
    "message": [string] Authorization issue
}
```

#### Messages

- You are not authorized to access this resource
> The token from Firebase is invalid
- Authorization header is missing
> There is no "Authorization" in the header request.
- Invalid authorization header format
> The "Authorization" header does not have "Bearer Token" format.

### Response `403`

Forbidden as only the owner can delete the invite code of an organization.

### Response `404`

The invite does not exist.

### Response `500`

Internal error with the server.

#### Body
```
{
    "error": "Internal Server Error",
    "message": "Internal Server Error"
}
```